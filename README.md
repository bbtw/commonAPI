# CommonAPI

## Getting Started

```bash
uv venv && source .venv/bin/activate
uv pip install -e ".[dev]"
cp .env.example .env
python main.py          # dev server at http://localhost:8000
pytest                  # run tests
```
